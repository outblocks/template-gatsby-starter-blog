plugins:
  - name: docker
    version: ">=0.1.0"
  - name: gcp
    version: ">=0.1.0"

dependencies:
  database:
    type: postgresql
    version: "13"

  media_storage:
    type: storage
    name: {{ .project.name }}

dns:
  - domains:
    - '*.${var.blog_domain}'
    - '${var.blog_domain}'
