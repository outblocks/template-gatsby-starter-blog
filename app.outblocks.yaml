name: gatsby-blog
url: ${var.blog_domain}
type: static

env:
  SITE_URL: ${var.blog_domain}

build:
  command: npm i && npm run build
  dir: build

run:
  command: npm i && npm run develop
  port: 8000

routing: gatsby
